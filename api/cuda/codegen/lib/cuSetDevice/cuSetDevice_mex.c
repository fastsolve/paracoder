/*
 * codegen/lib/cuSetDevice/cuSetDevice_mex.c
 *
 * Auxiliary code for mexFunction of cuSetDevice
 *
 * C source code generated by m2c.
 * %#m2c options:9f24c68e15bf2f81e606c54eee363e98
 *
 */

#ifdef MATLAB_MEX_FILE
#include "mex.h"
#else
#include "matrix.h"
#endif
/* Include the C header file generated by codegen in lib mode */
#include "cuSetDevice.h"
#include "m2c.c"

/* Include declaration of some helper functions. */
#include "lib2mex_helper.c"


static void __cuSetDevice_api(mxArray **plhs, const mxArray ** prhs) {
    int32_T              dev;
    int32_T             *errCode;
    boolean_T           *toplevel;

    /* Marshall in inputs and preallocate outputs */
    if (mxGetNumberOfElements(prhs[0]) && mxGetClassID(prhs[0]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("cuSetDevice:WrongInputType",
            "Input argument dev has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[0]) != 1)
        mexErrMsgIdAndTxt("cuSetDevice:WrongSizeOfInputArg",
            "Argument dev should be a scalar.");
    dev = *(int32_T*)mxGetData(prhs[0]);

    errCode = mxMalloc(sizeof(int32_T));

    toplevel = mxMalloc(sizeof(boolean_T));

    /* Invoke the target function */
    cuSetDevice(dev, errCode, toplevel);

    /* Deallocate input and marshall out function outputs */
    /* Nothing to be done for dev */
    plhs[0] = move_scalar_to_mxArray(errCode, mxINT32_CLASS);
    plhs[1] = move_scalar_to_mxArray(toplevel, mxLOGICAL_CLASS);

}


void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[]) {
    /* Temporary copy for mex outputs. */
    mxArray *outputs[2];
    int i;
    int nOutputs = (nlhs < 1 ? 1 : nlhs);

    if (nrhs == 1) {
        if (nlhs > 2)
            mexErrMsgIdAndTxt("cuSetDevice:TooManyOutputArguments",
                "Too many output arguments for entry-point cuSetDevice.\n");
        /* Call the API function. */
        __cuSetDevice_api(outputs, prhs);
    }
    else
        mexErrMsgIdAndTxt("cuSetDevice:WrongNumberOfInputs",
            "Incorrect number of input variables for entry-point cuSetDevice.");

    /* Copy over outputs to the caller. */
    for (i = 0; i < nOutputs; ++i) {
        plhs[i] = outputs[i];
    }
}
