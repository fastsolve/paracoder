/*
 * codegen/lib/cuGetDeviceCount/cuGetDeviceCount_mex.c
 *
 * Auxiliary code for mexFunction of cuGetDeviceCount
 *
 * C source code generated by m2c.
 * %#m2c options:a86f06273668c7ee1c254c6a92bd888a
 *
 */

#include "mex.h"
#if !defined(MATLAB_MEX_FILE) && defined(printf)
#undef printf
#endif
/* Include the C header file generated by codegen in lib mode */
#include "cuGetDeviceCount.h"
#include "m2c.c"

/* Include declaration of some helper functions. */
#include "lib2mex_helper.c"


static void __cuGetDeviceCount_api(mxArray **plhs, const mxArray ** prhs) {
    int32_T             *n;
    int32_T             *errCode;
    boolean_T           *toplevel;

    /* Marshall in inputs and preallocate outputs */
    n = mxMalloc(sizeof(int32_T));

    errCode = mxMalloc(sizeof(int32_T));

    toplevel = mxMalloc(sizeof(boolean_T));

    /* Invoke the target function */
    cuGetDeviceCount(n, errCode, toplevel);

    /* Deallocate input and marshall out function outputs */
    plhs[0] = move_scalar_to_mxArray(n, mxINT32_CLASS);
    plhs[1] = move_scalar_to_mxArray(errCode, mxINT32_CLASS);
    plhs[2] = move_scalar_to_mxArray(toplevel, mxLOGICAL_CLASS);

}


void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[]) {
    /* Temporary copy for mex outputs. */
    mxArray *outputs[3];
    int i;
    int nOutputs = (nlhs < 1 ? 1 : nlhs);

    if (nrhs == 0) {
        if (nlhs > 3)
            mexErrMsgIdAndTxt("cuGetDeviceCount:TooManyOutputArguments",
                "Too many output arguments for entry-point cuGetDeviceCount.\n");
        /* Call the API function. */
        __cuGetDeviceCount_api(outputs, prhs);
    }
    else
        mexErrMsgIdAndTxt("cuGetDeviceCount:WrongNumberOfInputs",
            "Incorrect number of input variables for entry-point cuGetDeviceCount.");

    /* Copy over outputs to the caller. */
    for (i = 0; i < nOutputs; ++i) {
        plhs[i] = outputs[i];
    }
}
