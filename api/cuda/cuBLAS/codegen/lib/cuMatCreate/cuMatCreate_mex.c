/*
 * cuMatCreate_mex.c
 *
 * Auxiliary code for mexFunction of cuMatCreate
 *
 * C source code generated by m2c.
 * %#m2c options:6a031e3c293c3ae79a56720776d939a9
 *
 */

#ifdef MATLAB_MEX_FILE
#include "mex.h"
#else
#include "matrix.h"
#endif
/* Include the C header file generated by codegen in lib mode */
#include "cuMatCreate.h"
#include "m2c.c"

/* Include declaration of some helper functions. */
#include "lib2mex_helper.c"


static mxArray *marshallout_struct0_T(struct0_T *pStruct) {
    const char           *fields[] = {"data", "type", "dims"};
    int                  one=1;
    mxArray              *mx = create_struct_mxArray(1, &one, 3, fields);
    int                  _dims[2];

    mxSetField(mx, 0, "data", copy_scalar_to_mxArray(&pStruct->data, mxUINT64_CLASS));
    mxSetField(mx, 0, "type", copy_scalar_to_mxArray(&pStruct->type, mxINT32_CLASS));
    _dims[0] = 1;
    _dims[1] = 2;
    mxSetField(mx, 0, "dims", copy_array_to_mxArray(pStruct->dims, mxINT32_CLASS, 2, _dims));
    return mx;
}



static void __cuMatCreate_api(mxArray **plhs, const mxArray ** prhs) {
    int32_T              m;
    int32_T              n;
    int32_T              type;
    struct0_T            mat;
    int32_T             *errCode;
    boolean_T           *toplevel;

    /* Marshall in inputs and preallocate outputs */
    if (mxGetNumberOfElements(prhs[0]) && mxGetClassID(prhs[0]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("cuMatCreate:WrongInputType",
            "Input argument m has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[0]) != 1)
        mexErrMsgIdAndTxt("cuMatCreate:WrongSizeOfInputArg",
            "Argument m should be a scalar.");
    m = *(int32_T*)mxGetData(prhs[0]);

    if (mxGetNumberOfElements(prhs[1]) && mxGetClassID(prhs[1]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("cuMatCreate:WrongInputType",
            "Input argument n has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[1]) != 1)
        mexErrMsgIdAndTxt("cuMatCreate:WrongSizeOfInputArg",
            "Argument n should be a scalar.");
    n = *(int32_T*)mxGetData(prhs[1]);

    if (mxGetNumberOfElements(prhs[2]) && mxGetClassID(prhs[2]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("cuMatCreate:WrongInputType",
            "Input argument type has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[2]) != 1)
        mexErrMsgIdAndTxt("cuMatCreate:WrongSizeOfInputArg",
            "Argument type should be a scalar.");
    type = *(int32_T*)mxGetData(prhs[2]);

    errCode = mxMalloc(sizeof(int32_T));

    toplevel = mxMalloc(sizeof(boolean_T));

    /* Invoke the target function */
    cuMatCreate(m, n, type, &mat, errCode, toplevel);

    /* Deallocate input and marshall out function outputs */
    /* Nothing to be done for m */
    /* Nothing to be done for n */
    /* Nothing to be done for type */
    plhs[0] = marshallout_struct0_T(&mat);
    plhs[1] = move_scalar_to_mxArray(errCode, mxINT32_CLASS);
    plhs[2] = move_scalar_to_mxArray(toplevel, mxLOGICAL_CLASS);

}

static void __cuMatCreate_2args_api(mxArray **plhs, const mxArray ** prhs) {
    int32_T              m;
    int32_T              n;
    struct0_T            vec;
    int32_T             *errCode;
    boolean_T           *toplevel;

    /* Marshall in inputs and preallocate outputs */
    if (mxGetNumberOfElements(prhs[0]) && mxGetClassID(prhs[0]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("cuMatCreate_2args:WrongInputType",
            "Input argument m has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[0]) != 1)
        mexErrMsgIdAndTxt("cuMatCreate_2args:WrongSizeOfInputArg",
            "Argument m should be a scalar.");
    m = *(int32_T*)mxGetData(prhs[0]);

    if (mxGetNumberOfElements(prhs[1]) && mxGetClassID(prhs[1]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("cuMatCreate_2args:WrongInputType",
            "Input argument n has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[1]) != 1)
        mexErrMsgIdAndTxt("cuMatCreate_2args:WrongSizeOfInputArg",
            "Argument n should be a scalar.");
    n = *(int32_T*)mxGetData(prhs[1]);

    errCode = mxMalloc(sizeof(int32_T));

    toplevel = mxMalloc(sizeof(boolean_T));

    /* Invoke the target function */
    cuMatCreate_2args(m, n, &vec, errCode, toplevel);

    /* Deallocate input and marshall out function outputs */
    /* Nothing to be done for m */
    /* Nothing to be done for n */
    plhs[0] = marshallout_struct0_T(&vec);
    plhs[1] = move_scalar_to_mxArray(errCode, mxINT32_CLASS);
    plhs[2] = move_scalar_to_mxArray(toplevel, mxLOGICAL_CLASS);

}


void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[]) {
    /* Temporary copy for mex outputs. */
    mxArray *outputs[3];
    int i;
    int nOutputs = (nlhs < 1 ? 1 : nlhs);

    if (nrhs == 3) {
        if (nlhs > 3)
            mexErrMsgIdAndTxt("cuMatCreate:TooManyOutputArguments",
                "Too many output arguments for entry-point cuMatCreate.\n");
        /* Call the API function. */
        __cuMatCreate_api(outputs, prhs);
    }
    else if (nrhs == 2) {
        if (nlhs > 3)
            mexErrMsgIdAndTxt("cuMatCreate_2args:TooManyOutputArguments",
                "Too many output arguments for entry-point cuMatCreate_2args.\n");
        /* Call the API function. */
        __cuMatCreate_2args_api(outputs, prhs);
    }
    else
        mexErrMsgIdAndTxt("cuMatCreate:WrongNumberOfInputs",
            "Incorrect number of input variables for entry-point cuMatCreate.");

    /* Copy over outputs to the caller. */
    for (i = 0; i < nOutputs; ++i) {
        plhs[i] = outputs[i];
    }
}
