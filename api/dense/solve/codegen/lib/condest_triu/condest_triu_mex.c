/*
 * dense/solve/codegen/lib/condest_triu/condest_triu_mex.c
 *
 * Auxiliary code for mexFunction of condest_triu
 *
 * C source code generated by m2c.
 * %#m2c options:4237cfb0121351654e7070afa3ad50f1
 *
 */

#include "mex.h"
#if !defined(MATLAB_MEX_FILE) && defined(printf)
#undef printf
#endif
/* Include the C header file generated by codegen in lib mode */
#include "condest_triu.h"
#include "m2c.c"

/* Include declaration of some helper functions. */
#include "lib2mex_helper.c"


static void marshallin_struct0_T(struct0_T *pStruct, const mxArray *mx, const char *mname) {
    mxArray             *sub_mx;

    if (!mxIsStruct(mx))
        M2C_error("marshallin_struct0_T:WrongType",
            "Input argument %s has incorrect data type; struct is expected.", mname);
    if (!mxGetField(mx, 0, "x"))
        M2C_error("marshallin_struct0_T:WrongType",
            "Input argument %s is missing the field x.", mname);
    if (!mxGetField(mx, 0, "z"))
        M2C_error("marshallin_struct0_T:WrongType",
            "Input argument %s is missing the field z.", mname);
    if (mxGetNumberOfFields(mx) > 2)
        M2C_warn("marshallin_struct0_T:ExtraFields",
            "Extra fields in %s and are ignored.", mname);

    sub_mx = mxGetField(mx, 0, "x");
    if (mxGetNumberOfElements(sub_mx) && mxGetClassID(sub_mx) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("marshallin_struct0_T:WrongInputType",
            "Input argument x has incorrect data type; double is expected.");
    if (mxGetNumberOfElements(sub_mx) && mxGetDimensions(sub_mx)[1] != 1) 
        mexErrMsgIdAndTxt("marshallin_struct0_T:WrongSizeOfInputArg",
            "Dimension 2 of x should be equal to 1.");
    pStruct->x = (emxArray_real_T*)mxMalloc(sizeof(emxArray_real_T));
    init_emxArray((emxArray__common*)(pStruct->x), 1);
    copy_mxArray_to_emxArray(sub_mx, (emxArray__common *)(pStruct->x), "x", 1);

    sub_mx = mxGetField(mx, 0, "z");
    if (mxGetNumberOfElements(sub_mx) && mxGetClassID(sub_mx) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("marshallin_struct0_T:WrongInputType",
            "Input argument z has incorrect data type; double is expected.");
    if (mxGetNumberOfElements(sub_mx) && mxGetDimensions(sub_mx)[1] != 1) 
        mexErrMsgIdAndTxt("marshallin_struct0_T:WrongSizeOfInputArg",
            "Dimension 2 of z should be equal to 1.");
    pStruct->z = (emxArray_real_T*)mxMalloc(sizeof(emxArray_real_T));
    init_emxArray((emxArray__common*)(pStruct->z), 1);
    copy_mxArray_to_emxArray(sub_mx, (emxArray__common *)(pStruct->z), "z", 1);
}
static void prealloc_struct0_T(struct0_T *pStruct) {


    pStruct->x = (emxArray_real_T*)mxMalloc(sizeof(emxArray_real_T));
    init_emxArray((emxArray__common*)(pStruct->x), 1);

    pStruct->z = (emxArray_real_T*)mxMalloc(sizeof(emxArray_real_T));
    init_emxArray((emxArray__common*)(pStruct->z), 1);
}
static mxArray *marshallout_struct0_T(struct0_T *pStruct) {
    const char           *fields[] = {"x", "z"};
    int                  one=1;
    mxArray              *mx = create_struct_mxArray(1, &one, 2, fields);

    mxSetField(mx, 0, "x", move_emxArray_to_mxArray((emxArray__common*)(pStruct->x), mxDOUBLE_CLASS));
    mxFree(pStruct->x->size);
    mxFree(pStruct->x);

    mxSetField(mx, 0, "z", move_emxArray_to_mxArray((emxArray__common*)(pStruct->z), mxDOUBLE_CLASS));
    mxFree(pStruct->z->size);
    mxFree(pStruct->z);

    return mx;
}



static void __condest_triu_api(mxArray **plhs, const mxArray ** prhs) {
    emxArray_real_T      R;
    int32_T              m;
    int32_T              maxiter;
    struct0_T            buf;
    real64_T             _kappa;

    /* Marshall in inputs and preallocate outputs */
    if (mxGetNumberOfElements(prhs[0]) && mxGetClassID(prhs[0]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("condest_triu:WrongInputType",
            "Input argument R has incorrect data type; double is expected.");
    alias_mxArray_to_emxArray(prhs[0], (emxArray__common *)(&R), "R", 2);

    if (mxGetNumberOfElements(prhs[1]) && mxGetClassID(prhs[1]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("condest_triu:WrongInputType",
            "Input argument m has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[1]) != 1)
        mexErrMsgIdAndTxt("condest_triu:WrongSizeOfInputArg",
            "Argument m should be a scalar.");
    m = *(int32_T*)mxGetData(prhs[1]);

    if (mxGetNumberOfElements(prhs[2]) && mxGetClassID(prhs[2]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("condest_triu:WrongInputType",
            "Input argument maxiter has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[2]) != 1)
        mexErrMsgIdAndTxt("condest_triu:WrongSizeOfInputArg",
            "Argument maxiter should be a scalar.");
    maxiter = *(int32_T*)mxGetData(prhs[2]);

    if (mxGetNumberOfElements(prhs[3]) && mxGetClassID(prhs[3]) != mxSTRUCT_CLASS)
        mexErrMsgIdAndTxt("condest_triu:WrongInputType",
            "Input argument buf has incorrect data type; struct is expected.");
    if (mxGetNumberOfElements(prhs[3]) != 1)
        mexErrMsgIdAndTxt("condest_triu:WrongSizeOfInputArg",
            "Argument buf should be a scalar.");
    marshallin_struct0_T(&buf, prhs[3], "buf");

    /* Invoke the target function */
    _kappa = condest_triu(&R, m, maxiter, &buf);

    /* Deallocate input and marshall out function outputs */
    free_emxArray((emxArray__common*)(&R));
    /* Nothing to be done for m */
    /* Nothing to be done for maxiter */
    plhs[1] = marshallout_struct0_T(&buf);
    plhs[0] = copy_scalar_to_mxArray(&_kappa, mxDOUBLE_CLASS);

}

static void __condest_triu_3args_api(mxArray **plhs, const mxArray ** prhs) {
    emxArray_real_T      R;
    int32_T              m;
    int32_T              maxiter;
    real64_T            *kappa;
    struct0_T            buf;

    /* Marshall in inputs and preallocate outputs */
    if (mxGetNumberOfElements(prhs[0]) && mxGetClassID(prhs[0]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("condest_triu_3args:WrongInputType",
            "Input argument R has incorrect data type; double is expected.");
    alias_mxArray_to_emxArray(prhs[0], (emxArray__common *)(&R), "R", 2);

    if (mxGetNumberOfElements(prhs[1]) && mxGetClassID(prhs[1]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("condest_triu_3args:WrongInputType",
            "Input argument m has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[1]) != 1)
        mexErrMsgIdAndTxt("condest_triu_3args:WrongSizeOfInputArg",
            "Argument m should be a scalar.");
    m = *(int32_T*)mxGetData(prhs[1]);

    if (mxGetNumberOfElements(prhs[2]) && mxGetClassID(prhs[2]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("condest_triu_3args:WrongInputType",
            "Input argument maxiter has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[2]) != 1)
        mexErrMsgIdAndTxt("condest_triu_3args:WrongSizeOfInputArg",
            "Argument maxiter should be a scalar.");
    maxiter = *(int32_T*)mxGetData(prhs[2]);

    kappa = (real64_T*)mxMalloc(sizeof(real64_T));
    prealloc_struct0_T(&buf);

    /* Invoke the target function */
    condest_triu_3args(&R, m, maxiter, kappa, &buf);

    /* Deallocate input and marshall out function outputs */
    free_emxArray((emxArray__common*)(&R));
    /* Nothing to be done for m */
    /* Nothing to be done for maxiter */
    plhs[0] = move_scalar_to_mxArray(kappa, mxDOUBLE_CLASS);
    plhs[1] = marshallout_struct0_T(&buf);

}


void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[]) {
    /* Temporary copy for mex outputs. */
    mxArray *outputs[2] = {NULL, NULL};
    int i;
    int nOutputs = (nlhs < 1 ? 1 : nlhs);

    if (nrhs == 4) {
        if (nlhs > 2)
            mexErrMsgIdAndTxt("condest_triu:TooManyOutputArguments",
                "Too many output arguments for entry-point condest_triu.\n");
        /* Call the API function. */
        __condest_triu_api(outputs, prhs);
    }
    else if (nrhs == 3) {
        if (nlhs > 2)
            mexErrMsgIdAndTxt("condest_triu_3args:TooManyOutputArguments",
                "Too many output arguments for entry-point condest_triu_3args.\n");
        /* Call the API function. */
        __condest_triu_3args_api(outputs, prhs);
    }
    else
        mexErrMsgIdAndTxt("condest_triu:WrongNumberOfInputs",
            "Incorrect number of input variables for entry-point condest_triu.");

    /* Copy over outputs to the caller. */
    for (i = 0; i < nOutputs; ++i) {
        plhs[i] = outputs[i];
    }
}
