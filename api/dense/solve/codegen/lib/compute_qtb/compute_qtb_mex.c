/*
 * dense/solve/codegen/lib/compute_qtb/compute_qtb_mex.c
 *
 * Auxiliary code for mexFunction of compute_qtb
 *
 * C source code generated by m2c.
 * %#m2c options:a0e95787e4fd851490dd27c24cd976e8
 *
 */

#include "mex.h"
#if !defined(MATLAB_MEX_FILE) && defined(printf)
#undef printf
#endif
/* Include the C header file generated by codegen in lib mode */
#include "compute_qtb.h"
#include "m2c.c"

/* Include declaration of some helper functions. */
#include "lib2mex_helper.c"


static void __compute_qtb_api(mxArray **plhs, const mxArray ** prhs) {
    emxArray_real_T      Q;
    emxArray_real_T      bs;
    int32_T              ncols;

    /* Marshall in inputs and preallocate outputs */
    if (mxGetNumberOfElements(prhs[0]) && mxGetClassID(prhs[0]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("compute_qtb:WrongInputType",
            "Input argument Q has incorrect data type; double is expected.");
    alias_mxArray_to_emxArray(prhs[0], (emxArray__common *)(&Q), "Q", 2);

    if (mxGetNumberOfElements(prhs[1]) && mxGetClassID(prhs[1]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("compute_qtb:WrongInputType",
            "Input argument bs has incorrect data type; double is expected.");
    copy_mxArray_to_emxArray(prhs[1], (emxArray__common *)(&bs), "bs", 2);

    if (mxGetNumberOfElements(prhs[2]) && mxGetClassID(prhs[2]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("compute_qtb:WrongInputType",
            "Input argument ncols has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[2]) != 1)
        mexErrMsgIdAndTxt("compute_qtb:WrongSizeOfInputArg",
            "Argument ncols should be a scalar.");
    ncols = *(int32_T*)mxGetData(prhs[2]);

    /* Invoke the target function */
    compute_qtb(&Q, &bs, ncols);

    /* Deallocate input and marshall out function outputs */
    free_emxArray((emxArray__common*)(&Q));
    plhs[0] = move_emxArray_to_mxArray((emxArray__common*)(&bs), mxDOUBLE_CLASS);
    mxFree(bs.size);
    /* Nothing to be done for ncols */

}


void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[]) {
    if (nrhs == 3) {
        if (nlhs > 1)
            mexErrMsgIdAndTxt("compute_qtb:TooManyOutputArguments",
                "Too many output arguments for entry-point compute_qtb.\n");
        /* Call the API function. */
        __compute_qtb_api(plhs, prhs);
    }
    else
        mexErrMsgIdAndTxt("compute_qtb:WrongNumberOfInputs",
            "Incorrect number of input variables for entry-point compute_qtb.");
}
