/*
 * dense/solve/codegen/lib/qr_rescale/qr_rescale_mex.c
 *
 * Auxiliary code for mexFunction of qr_rescale
 *
 * C source code generated by m2c.
 * %#m2c options:39218ea39571dd525e90b147b8a92bff
 *
 */

#include "mex.h"
#if !defined(MATLAB_MEX_FILE) && defined(printf)
#undef printf
#endif
/* Include the C header file generated by codegen in lib mode */
#include "qr_rescale.h"
#include "m2c.c"

/* Include declaration of some helper functions. */
#include "lib2mex_helper.c"


static void __qr_rescale_api(mxArray **plhs, const mxArray ** prhs) {
    emxArray_real_T      A;
    int32_T              ncols;
    real64_T             tol;
    int32_T             *rnk;
    emxArray_real_T      V;

    /* Marshall in inputs and preallocate outputs */
    if (mxGetNumberOfElements(prhs[0]) && mxGetClassID(prhs[0]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("qr_rescale:WrongInputType",
            "Input argument A has incorrect data type; double is expected.");
    copy_mxArray_to_emxArray(prhs[0], (emxArray__common *)(&A), "A", 2);

    if (mxGetNumberOfElements(prhs[1]) && mxGetClassID(prhs[1]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("qr_rescale:WrongInputType",
            "Input argument ncols has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[1]) != 1)
        mexErrMsgIdAndTxt("qr_rescale:WrongSizeOfInputArg",
            "Argument ncols should be a scalar.");
    ncols = *(int32_T*)mxGetData(prhs[1]);

    if (mxGetNumberOfElements(prhs[2]) && mxGetClassID(prhs[2]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("qr_rescale:WrongInputType",
            "Input argument tol has incorrect data type; double is expected.");
    if (mxGetNumberOfElements(prhs[2]) != 1)
        mexErrMsgIdAndTxt("qr_rescale:WrongSizeOfInputArg",
            "Argument tol should be a scalar.");
    tol = *(real64_T*)mxGetData(prhs[2]);

    rnk = (int32_T*)mxMalloc(sizeof(int32_T));
    init_emxArray((emxArray__common*)(&V), 1);

    /* Invoke the target function */
    qr_rescale(&A, ncols, tol, rnk, &V);

    /* Deallocate input and marshall out function outputs */
    plhs[0] = move_emxArray_to_mxArray((emxArray__common*)(&A), mxDOUBLE_CLASS);
    mxFree(A.size);
    /* Nothing to be done for ncols */
    /* Nothing to be done for tol */
    plhs[1] = move_scalar_to_mxArray(rnk, mxINT32_CLASS);
    plhs[2] = move_emxArray_to_mxArray((emxArray__common*)(&V), mxDOUBLE_CLASS);
    mxFree(V.size);

}


void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[]) {
    /* Temporary copy for mex outputs. */
    mxArray *outputs[3] = {NULL, NULL, NULL};
    int i;
    int nOutputs = (nlhs < 1 ? 1 : nlhs);

    if (nrhs == 3) {
        if (nlhs > 3)
            mexErrMsgIdAndTxt("qr_rescale:TooManyOutputArguments",
                "Too many output arguments for entry-point qr_rescale.\n");
        /* Call the API function. */
        __qr_rescale_api(outputs, prhs);
    }
    else
        mexErrMsgIdAndTxt("qr_rescale:WrongNumberOfInputs",
            "Incorrect number of input variables for entry-point qr_rescale.");

    /* Copy over outputs to the caller. */
    for (i = 0; i < nOutputs; ++i) {
        plhs[i] = outputs[i];
    }
}
