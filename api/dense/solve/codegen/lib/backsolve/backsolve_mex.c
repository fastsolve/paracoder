/*
 * dense/solve/codegen/lib/backsolve/backsolve_mex.c
 *
 * Auxiliary code for mexFunction of backsolve
 *
 * C source code generated by m2c.
 * %#m2c options:66f13a6edfe6f431dd130f54f74bf3dd
 *
 */

#include "mex.h"
#if !defined(MATLAB_MEX_FILE) && defined(printf)
#undef printf
#endif
/* Include the C header file generated by codegen in lib mode */
#include "backsolve.h"
#include "backsolve_types.h"
#include "m2c.c"

/* Include declaration of some helper functions. */
#include "lib2mex_helper.c"


static void __backsolve_api(mxArray **plhs, const mxArray ** prhs) {
    emxArray_real_T      R;
    emxArray_real_T      bs;
    int32_T              cend;
    emxArray_real_T      ws;

    /* Marshall in inputs and preallocate outputs */
    if (mxGetNumberOfElements(prhs[0]) && mxGetClassID(prhs[0]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("backsolve:WrongInputType",
            "Input argument R has incorrect data type; double is expected.");
    alias_mxArray_to_emxArray(prhs[0], (emxArray__common *)(&R), "R", 2);

    if (mxGetNumberOfElements(prhs[1]) && mxGetClassID(prhs[1]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("backsolve:WrongInputType",
            "Input argument bs has incorrect data type; double is expected.");
    copy_mxArray_to_emxArray(prhs[1], (emxArray__common *)(&bs), "bs", 2);

    if (mxGetNumberOfElements(prhs[2]) && mxGetClassID(prhs[2]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("backsolve:WrongInputType",
            "Input argument cend has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[2]) != 1)
        mexErrMsgIdAndTxt("backsolve:WrongSizeOfInputArg",
            "Argument cend should be a scalar.");
    cend = *(int32_T*)mxGetData(prhs[2]);

    if (mxGetNumberOfElements(prhs[3]) && mxGetClassID(prhs[3]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("backsolve:WrongInputType",
            "Input argument ws has incorrect data type; double is expected.");
    if (mxGetNumberOfElements(prhs[3]) && mxGetDimensions(prhs[3])[1] != 1) 
        mexErrMsgIdAndTxt("backsolve:WrongSizeOfInputArg",
            "Dimension 2 of ws should be equal to 1.");
    alias_mxArray_to_emxArray(prhs[3], (emxArray__common *)(&ws), "ws", 1);

    /* Invoke the target function */
    backsolve(&R, &bs, cend, &ws);

    /* Deallocate input and marshall out function outputs */
    free_emxArray((emxArray__common*)(&R));
    plhs[0] = move_emxArray_to_mxArray((emxArray__common*)(&bs), mxDOUBLE_CLASS);
    mxFree(bs.size);
    /* Nothing to be done for cend */
    free_emxArray((emxArray__common*)(&ws));

}

static void __backsolve_2args_api(mxArray **plhs, const mxArray ** prhs) {
    emxArray_real_T      R;
    emxArray_real_T      bs;

    /* Marshall in inputs and preallocate outputs */
    if (mxGetNumberOfElements(prhs[0]) && mxGetClassID(prhs[0]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("backsolve_2args:WrongInputType",
            "Input argument R has incorrect data type; double is expected.");
    alias_mxArray_to_emxArray(prhs[0], (emxArray__common *)(&R), "R", 2);

    if (mxGetNumberOfElements(prhs[1]) && mxGetClassID(prhs[1]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("backsolve_2args:WrongInputType",
            "Input argument bs has incorrect data type; double is expected.");
    copy_mxArray_to_emxArray(prhs[1], (emxArray__common *)(&bs), "bs", 2);

    /* Invoke the target function */
    backsolve_2args(&R, &bs);

    /* Deallocate input and marshall out function outputs */
    free_emxArray((emxArray__common*)(&R));
    plhs[0] = move_emxArray_to_mxArray((emxArray__common*)(&bs), mxDOUBLE_CLASS);
    mxFree(bs.size);

}

static void __backsolve_3args_api(mxArray **plhs, const mxArray ** prhs) {
    emxArray_real_T      R;
    emxArray_real_T      bs;
    int32_T              cend;

    /* Marshall in inputs and preallocate outputs */
    if (mxGetNumberOfElements(prhs[0]) && mxGetClassID(prhs[0]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("backsolve_3args:WrongInputType",
            "Input argument R has incorrect data type; double is expected.");
    alias_mxArray_to_emxArray(prhs[0], (emxArray__common *)(&R), "R", 2);

    if (mxGetNumberOfElements(prhs[1]) && mxGetClassID(prhs[1]) != mxDOUBLE_CLASS)
        mexErrMsgIdAndTxt("backsolve_3args:WrongInputType",
            "Input argument bs has incorrect data type; double is expected.");
    copy_mxArray_to_emxArray(prhs[1], (emxArray__common *)(&bs), "bs", 2);

    if (mxGetNumberOfElements(prhs[2]) && mxGetClassID(prhs[2]) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("backsolve_3args:WrongInputType",
            "Input argument cend has incorrect data type; int32 is expected.");
    if (mxGetNumberOfElements(prhs[2]) != 1)
        mexErrMsgIdAndTxt("backsolve_3args:WrongSizeOfInputArg",
            "Argument cend should be a scalar.");
    cend = *(int32_T*)mxGetData(prhs[2]);

    /* Invoke the target function */
    backsolve_3args(&R, &bs, cend);

    /* Deallocate input and marshall out function outputs */
    free_emxArray((emxArray__common*)(&R));
    plhs[0] = move_emxArray_to_mxArray((emxArray__common*)(&bs), mxDOUBLE_CLASS);
    mxFree(bs.size);
    /* Nothing to be done for cend */

}


void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[]) {
    if (nrhs == 4) {
        if (nlhs > 1)
            mexErrMsgIdAndTxt("backsolve:TooManyOutputArguments",
                "Too many output arguments for entry-point backsolve.\n");
        /* Call the API function. */
        __backsolve_api(plhs, prhs);
    }
    else if (nrhs == 2) {
        if (nlhs > 1)
            mexErrMsgIdAndTxt("backsolve_2args:TooManyOutputArguments",
                "Too many output arguments for entry-point backsolve_2args.\n");
        /* Call the API function. */
        __backsolve_2args_api(plhs, prhs);
    }
    else if (nrhs == 3) {
        if (nlhs > 1)
            mexErrMsgIdAndTxt("backsolve_3args:TooManyOutputArguments",
                "Too many output arguments for entry-point backsolve_3args.\n");
        /* Call the API function. */
        __backsolve_3args_api(plhs, prhs);
    }
    else
        mexErrMsgIdAndTxt("backsolve:WrongNumberOfInputs",
            "Incorrect number of input variables for entry-point backsolve.");
}
