classdef m2c_gpuArray
    properties (SetAccess = private)
        data = int64(0)
        type = ''
        dims = int32(0)
        counter = int32(0)
    end

    % TODO: Create reference counters
end
